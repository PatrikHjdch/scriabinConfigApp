<UserControl x:Class="scriabinWPF.MapProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:scriabinWPF" d:DataContext="{d:DesignInstance Type=local:MapProfileViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid>
        <ScrollViewer x:Name="LinkScrollViewer" Grid.Column="0" Grid.Row="0">
            <StackPanel x:Name="LinkStackPanel" Margin="8" >
                <StackPanel x:Name="ProfileHeaderStackPanel" Orientation="Horizontal">
                    <TextBlock Text="{Binding ID}" FontSize="16" Margin="12,8,8,8"/>
                    <TextBox x:Name="ProfileName" Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" FontSize="16" Margin="8" Width="240" HorizontalAlignment="Left" AcceptsReturn="True"/>
                </StackPanel>
                <ItemsControl ItemsSource="{Binding Links}">
                    <ItemsControl.Resources>
                        <DataTemplate DataType="{x:Type local:NoteLinkViewModel}">
                            <local:NoteLinkView DeleteLinkClicked="OnDeleteLinkClicked" Padding="2"/>
                        </DataTemplate>
                        <DataTemplate DataType="{x:Type local:NoteOnLinkViewModel}">
                            <local:NoteOnLinkView DeleteLinkClicked="OnDeleteLinkClicked" Padding="2"/>
                        </DataTemplate>
                        <DataTemplate DataType="{x:Type local:NoteOffLinkViewModel}">
                            <local:NoteOffLinkView DeleteLinkClicked="OnDeleteLinkClicked" Padding="2"/>
                        </DataTemplate>
                        <DataTemplate DataType="{x:Type local:ControlChangeLinkViewModel}">
                            <local:ControlChangeLinkView DeleteLinkClicked="OnDeleteLinkClicked" Padding="2"/>
                        </DataTemplate>
                    </ItemsControl.Resources>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type local:AbstractLinkViewModel}">
                            <ContentControl Content="{Binding}" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <Grid x:Name="AddLinkButtonHolder" >
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0">
                        <Button x:Name="AddNoteLinkButton" Content="Add Note Link" Height="78" Width="150" Margin="4" Click="AddNoteLink_Click" Padding="2"/>
                        <Button x:Name="AddNoteOnLinkButton" Content="Add Note On Link" Height="78" Width="150" Margin="4" Click="AddNoteOnLink_Click" Padding="2"/>
                        <Button x:Name="AddNoteOffLinkButton" Content="Add Note Off Link" Height="78" Width="150" Margin="4" Click="AddNoteOffLink_Click" Padding="2"/>
                        <Button x:Name="AddControlChangeLinkButton" Content="Add Control Change Link" Height="78" Width="150" Margin="4" Click="AddControlChangeLink_Click" Padding="2"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
